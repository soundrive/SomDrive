{
  "entities": {
    "Artist": {
      "title": "Artist",
      "description": "Information about an artist or band profile",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The unique slug ID of the artist profile" },
        "name": { "type": "string", "description": "The primary artistic name or band name" },
        "avatarUrl": { "type": "string", "description": "Profile picture storage or web URL" },
        "city": { "type": "string", "description": "Location formatted as City - State" },
        "genre": { "type": "string", "description": "Main musical genre" },
        "whatsapp": { "type": "string", "description": "Cleaned telephone number for messaging" },
        "instagram": { "type": "string", "description": "Instagram tag handle" },
        "email": { "type": "string", "description": "Registered email details" },
        "bio": { "type": "string", "description": "Brief biography or summary of achievements" },
        "plan": { "type": "string", "enum": ["free", "pro", "premium"], "description": "Assigned membership tier" },
        "subscriptionDate": { "type": "string", "description": "ISO timestamp indicating when subscription started" },
        "subscriptionStatus": { "type": "string", "enum": ["ativo", "pendente", "cancelado"], "description": "Current active billing state" },
        "createdAt": { "type": "string", "description": "Account registration time" }
      },
      "required": ["userId", "name", "email", "plan", "createdAt"]
    },
    "Music": {
      "title": "Music",
      "description": "A single track uploaded to the artist repository",
      "type": "object",
      "properties": {
        "trackId": { "type": "string", "description": "Unique identifier of the music track" },
        "artistId": { "type": "string", "description": "Reference lookup linking to the artist profile UID" },
        "title": { "type": "string", "description": "The name or title of the composition" },
        "composer": { "type": "string", "description": "Author of the composition lyrics or melody" },
        "singer": { "type": "string", "description": "Featured singer or band name" },
        "genre": { "type": "string", "description": "Specific sub-genre of the composition" },
        "description": { "type": "string", "description": "Brief note or pitch about the track" },
        "audioUrl": { "type": "string", "description": "Firebase Storage direct music playback URL" },
        "coverUrl": { "type": "string", "description": "Firebase Storage image thumbnail URL" },
        "lyrics": { "type": "string", "description": "Aesthetic lyrics formatted with verses and choruses" },
        "playsCount": { "type": "integer", "description": "Aesthetic meter of active fan impressions" },
        "createdAt": { "type": "string", "description": "ISO timestamp of upload" }
      },
      "required": ["trackId", "artistId", "title", "audioUrl", "playsCount", "createdAt"]
    },
    "Analytics": {
      "title": "Analytics",
      "description": "Consolidated statistics counting engagement with an artist profile",
      "type": "object",
      "properties": {
        "artistId": { "type": "string", "description": "Reference UID linking to the artist profile" },
        "viewsCount": { "type": "integer", "description": "Total visits logged on public page" },
        "whatsappClicks": { "type": "integer", "description": "Sum of direct conversion triggers" }
      },
      "required": ["artistId", "viewsCount", "whatsappClicks"]
    },
    "AudioFile": {
      "title": "AudioFile",
      "description": "Information about a deduplicated physical audio file in R2",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier of the audio file" },
        "audioHash": { "type": "string", "description": "SHA-256 hash of the MP3 file" },
        "audioUrl": { "type": "string", "description": "Direct R2 URL for streaming" },
        "storagePath": { "type": "string", "description": "The path/key inside Cloudflare R2 storage" },
        "fileSize": { "type": "integer", "description": "The size of the audio file in bytes" },
        "mimeType": { "type": "string", "description": "The MIME type of the audio file" },
        "originalFileName": { "type": "string", "description": "The original name of the uploaded file" },
        "createdBy": { "type": "string", "description": "Owner userId of the artist who first uploaded this file" },
        "createdAt": { "type": "string", "description": "ISO timestamp indicating when the file records were created" },
        "usageCount": { "type": "integer", "description": "The number of songs currently referencing this physical file" }
      },
      "required": ["id", "audioHash", "audioUrl", "storagePath", "createdBy", "createdAt", "usageCount"]
    },
    "Song": {
      "title": "Song",
      "description": "A single track item created by a user containing metadata and pointing to an audio file reference",
      "type": "object",
      "properties": {
        "songId": { "type": "string", "description": "Unique identifier of the song" },
        "ownerId": { "type": "string", "description": "The artist who owns this song entry" },
        "title": { "type": "string", "description": "Friendly title of the song" },
        "composer": { "type": "string", "description": "The composer of the song" },
        "partners": { "type": "string", "description": "Co-composers or partners split list" },
        "performer": { "type": "string", "description": "Featured singer or music interpreter" },
        "genre": { "type": "string", "description": "Musical genre" },
        "lyrics": { "type": "string", "description": "Song lyrics transcript" },
        "description": { "type": "string", "description": "About or pitch details" },
        "audioFileId": { "type": "string", "description": "Reference ID linking to the exact AudioFile entry" },
        "audioUrl": { "type": "string", "description": "Direct streaming link (from AudioFile)" },
        "storagePath": { "type": "string", "description": "The path/key inside Cloudflare R2 storage (from AudioFile)" },
        "storageProvider": { "type": "string", "description": "The storage provider technology used" },
        "fileSize": { "type": "integer", "description": "File size in bytes" },
        "mimeType": { "type": "string", "description": "The MIME type of the file" },
        "originalFileName": { "type": "string", "description": "Name of the original file" },
        "plays": { "type": "integer", "description": "Counter tracking streaming listens" },
        "createdAt": { "type": "string", "description": "Date of track catalog insert" },
        "updatedAt": { "type": "string", "description": "Date of track last modification" }
      },
      "required": ["songId", "ownerId", "title", "audioFileId", "audioUrl", "storagePath", "plays", "createdAt", "updatedAt"]
    }
  },
  "firestore": {
    "/artists/{artistId}": {
      "schema": "Artist",
      "description": "Public and private profile data for registered composers"
    },
    "/artists/{artistId}/musics/{musicId}": {
      "schema": "Music",
      "description": "Catalog of music tracks published under a specific artist profile"
    },
    "/analytics/{artistId}": {
      "schema": "Analytics",
      "description": "Engagement counters tracking individual metrics"
    },
    "/audioFiles/{audioFileId}": {
      "schema": "AudioFile",
      "description": "Listing of physical audio assets indexed by SHA-256 hash to prevent duplicates"
    },
    "/songs/{songId}": {
      "schema": "Song",
      "description": "Comprehensive index of user compositions with unique metrics"
    }
  }
}
